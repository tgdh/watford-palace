@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@{
    Layout = "MasterSpektrix.cshtml";
	
	var siteUrl = "https://" +  Request.Url.Host.ToString() + Request.RawUrl.ToString();
	
	if( !Request.IsSecureConnection && CurrentPage.DocumentTypeAlias == "Checkout" )
	{
		Response.Redirect(siteUrl);
	} 
}

@*
    secure url:
    https://system.spektrix.com/hulltruck/website/secure/myaccount.aspx
*@


<iframe
  name="SpektrixIFrame"
  id="SpektrixIFrame"
  frameborder="0"
  onload="scroll(0,0);"
  src="https://system.spektrix.com/watfordpalace/website/secure/checkout.aspx?resize=true"
  style="width: 100%; max-width: 100rem; margin: 0 auto; display: block; height: 1000px;">
</iframe>